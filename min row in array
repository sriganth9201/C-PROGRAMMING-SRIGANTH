#include <stdio.h>

int main() {
    int a[3][3], i, j, min;

    for (i = 0; i < 3; i++)
        for (j = 0; j < 3; j++)
            scanf("%d", &a[i][j]);

    for (i = 0; i < 3; i++) {
        min = a[i][0];
        for (j = 1; j < 3; j++) {
            if (a[i][j] < min)
                min = a[i][j];
        }
        printf("%d ", min);
    }

    return 0;
}
