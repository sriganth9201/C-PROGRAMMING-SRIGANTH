#include <stdio.h>
int main() {
    int a[3][3], i, j, max;
    for (i = 0; i < 3; i++)
        for (j = 0; j < 3; j++)
            scanf("%d", &a[i][j]);
    for (j = 0; j < 3; j++) {
        max = a[0][j];
        for (i = 1; i < 3; i++) {
            if (a[i][j] > max)
                max = a[i][j];
        }
        printf("%d ", max);
    }
    return 0;
}
